{
  "name": "ai-scent-scripts",
  "version": "1.0.0",
  "type": "module",
  "description": "AI Scent data ingestion scripts",
  "scripts": {
    "build": "tsc",
    "ingest": "tsc && node dist/ingest.js",
    "ingest:watch": "tsc -w"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  },
  "optionalDependencies": {
    "playwright": "^1.57.0"
  }
}
